@model IEnumerable<WebApp.Models.MyGreatTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GreatField)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GreatField)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MyGreatTableId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MyGreatTableId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MyGreatTableId })
        </td>
    </tr>
}

</table>
